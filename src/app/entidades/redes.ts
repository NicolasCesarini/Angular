export class Redes {
    //atributos
    id?: number;
    red: string;
    link: string;

    //constructor con parámetros
    constructor(red: string, link: string){
        this.red = red;
        this.link = link
    }
}
